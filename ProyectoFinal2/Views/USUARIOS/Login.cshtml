<!DOCTYPE html>
<html>
<head>
    <title>Login - Sistema de Inversiones</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 40px;
            background: #f5f5f5;
        }

        .container {
            max-width: 400px;
            margin: 0 auto;
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }

        .form-group {
            margin: 15px 0;
        }

        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        input {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            box-sizing: border-box;
        }

        button {
            width: 100%;
            padding: 12px;
            background: #007cba;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }

            button:hover {
                background: #005a87;
            }

        .error {
            color: red;
            margin: 10px 0;
            padding: 10px;
            background: #ffeaea;
            border-radius: 5px;
        }

        .success {
            color: green;
            margin: 10px 0;
            padding: 10px;
            background: #eaffea;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>🏦 Sistema de Inversiones</h2>
        <p>Proyecto Programación 2 - UMG</p>

        <form action="/USUARIOS/Login" method="post">
            <input type="hidden" name="__RequestVerificationToken" value="debug-token" />

            <div class="form-group">
                <label for="usuario">Usuario:</label>
                <input type="text" id="usuario" name="usuario" value="USR_PRG2_A" required />
            </div>

            <div class="form-group">
                <label for="contrasena">Contraseña:</label>
                <input type="password" id="contrasena" name="contrasena" value="umg123" required />
            </div>

            <button type="submit">Iniciar Sesión</button>
        </form>

        <div style="margin-top: 20px; text-align: center;">
            <a href="/USUARIOS/Create">¿No tienes cuenta? Regístrate</a>
        </div>

        <!-- MENSAJE DE ESTADO -->
        <div id="status-message" style="margin-top: 20px;"></div>
    </div>

    <script>
        // Mostrar mensaje basado en parámetros URL
        const urlParams = new URLSearchParams(window.location.search);
        const error = urlParams.get('error');
        const success = urlParams.get('success');

        const statusDiv = document.getElementById('status-message');

        if (error) {
            statusDiv.innerHTML = '<div class="error">' + decodeURIComponent(error) + '</div>';
        }
        if (success) {
            statusDiv.innerHTML = '<div class="success">' + decodeURIComponent(success) + '</div>';
        }
    </script>
</body>
</html>